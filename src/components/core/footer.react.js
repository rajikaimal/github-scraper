import React from 'react';

const footer = React.createClass({
    render: function () {
        return (
            <div>
                <footer> boilerplate created with <a href="https://github.com/rajikaimal/flux-cli"> flux-cli </a> </footer>
            </div>
        );
    }
});

export default footer;